{
  "name": "@vulcanjs/permissions",
  "version": "0.5.3",
  "description": "Vulcan permissions",
  "main": "./dist/index.js",
  "files": [
    "dist/"
  ],
  "type": "module",
  "exports": {
    ".": {
      "node": "./dist/index.cjs",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:tsup": "tsup",
    "build:types": "tsc --emitDeclarationOnly --declaration --project tsconfig.build.json # Tsup generates .d.ts but we still need this for .d.ts.map",
    "build:webpack": "webpack --config ./webpack.config.js",
    "build": "yarn run build:tsup && yarn run build:types # yarn run build:common && yarn run build:server"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@vulcanjs/model": "^0.5.3",
    "@vulcanjs/schema": "^0.5.3",
    "lodash": "^4.17.21"
  },
  "peerDependencies": {
    "react": ">= 16.13.1",
    "react-dom": ">= 16.13.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.168"
  }
}
