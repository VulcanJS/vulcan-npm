import React from "react";
import PropTypes from "prop-types";

export const FormNestedDivider = ({ label, addItem }) => <div />;

FormNestedDivider.propTypes = {
  label: PropTypes.string,
  addItem: PropTypes.func,
};
